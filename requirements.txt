# Fisherboy v1 — núcleo liviano. Runtime.
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pyyaml>=6.0
trafilatura>=1.12
redis>=5.0
lxml>=5.0               # parsing auto-reparable (Capa 4)
cssselect>=1.2          # selectores CSS para el parsing auto-reparable
cryptography>=42.0      # modo reversible: cifra la tabla de mapeo (Fernet)
jsonschema>=4.20        # valida la extracción estructurada del LLM
prometheus-client>=0.20 # métricas (Capa 8)

# MCP (sidekick): n8n / Claude Code / Escriba encolan por MCP. Opcional para REST.
fastmcp>=2.0

# --- Tiers de fetch altos (Capa 3, ADR-006). El router los detecta solos.
#   tier 1 (TLS fingerprint):
curl_cffi>=0.7
#   tier 2 (stealth browser) + captura de API/XHR: patchright (Chromium, stealth):
patchright>=1.45
#   tier 3 (browser último recurso): Playwright. El binario de Chromium se instala en
#   el Dockerfile (`playwright install chromium`). Local: corré ese comando una vez.
playwright>=1.47
# nodriver>=0.38          # alternativa a Playwright para tier 3 (usa Chrome del sistema)
# camoufox[geoip]>=0.4    # alternativa a patchright para tier 2

# --- Conversión LLM-ready RECOMENDADA (Capa 5). Si falta, cae a Trafilatura.
crawl4ai>=0.4            # markdown "fit" (poda nav/boilerplate por densidad) — lo mejor para LLM
browser-cookie3>=0.19   # leer cookies del navegador local (reemplaza la extensión; solo standalone)
# scrapling>=0.2         # parsing adaptivo + stealth fetcher (alternativa a parsing/adaptive.py)

# --- Capa 7 opcional. El sistema degrada sin ella.
# psycopg[binary]>=3.1   # persistencia Postgres + pgvector
